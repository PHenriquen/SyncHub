# Contributing

1. Create a focused branch.
2. Keep domain changes inside the correct module.
3. Add validation contracts for new inputs.
4. Run `npm run check`, `npm run test` and `npm run build`.
5. Do not commit generated folders, secrets or local databases.
6. Keep source files below 450 lines. Split by responsibility before reaching the limit.
